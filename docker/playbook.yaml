#cd ~/Documents/Tech/Ansible-Playbook/docker
#
#ansible-vault encrypt /Users/troy/Documents/Tech/Ansible-Playbook/docker/docker_install/vars/main.yaml
#ansible-vault decrypt /Users/troy/Documents/Tech/Ansible-Playbook/docker/docker_install/vars/main.yaml
#
#ansible-playbook --ask-vault-password playbook.yaml -i inventory.yaml --ask-become-pass --key-file ~/.ssh/id_ecdsa
#ansible-playbook --ask-vault-password playbook.yaml -i inventory.yaml

---
- name: Install Docker Homelab
  hosts: virtual_machines
  become: true
  roles:
    - docker_install
    - ufw_install
    - alertmanager_deploy
    #- audiobookshelf_deploy
    #- bazarr_deploy
    - dozzle_deploy
    - filebrowser_deploy
    #- ghost_deploy
    #- gitea_deploy
    #- gotify_deploy
    #- grafana_deploy
    - influxdb_deploy
    #- jellyfin_deploy
    #- jellyseerr_deploy
    #- kavita_deploy
    - loki_deploy
    #- mariadb_deploy
    #- memos_deploy
    #- mkvtoolnix_deploy
    #- myspeed_deploy
    - node-exporter_deploy
    #- overseerr_deploy
    #- paperless-ngx_deploy
    #- photoprism_deploy
    #- pihole_deploy
    #- plex_deploy
    - prometheus_deploy
    - promtail_deploy
    #- prowlarr_deploy
    #- radarr_deploy
    #- redis_deploy
    #- sonarr_deploy
    #- syncthing_deploy
    #- tautulli_deploy
    - telegraf_deploy
    #- transmission_deploy
    #- uptime-kuma_deploy
    - vaultwarden_deploy
    - watchtower_deploy