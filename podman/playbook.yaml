#cd $HOME/Documents/Tech/Ansible-Playbook/podman

#ansible-vault encrypt /Users/troy/Documents/Tech/Ansible-Playbook/podman/podman_install/vars/main.yaml
#ansible-vault decrypt /Users/troy/Documents/Tech/Ansible-Playbook/podman/podman_install/vars/main.yaml

#ansible-playbook --ask-vault-password playbook.yaml -i inventory.yaml --ask-become-pass --key-file $HOME/.ssh/id_ed25519
#ansible-playbook --ask-vault-password playbook.yaml -i inventory.yaml

---
- name: Install Podman Homelab
  hosts: virtual_machines
  become: true
  roles:
    - podman_install
    - firewalld_install
    #- alertmanager_deploy
    #- audiobookshelf_deploy
    #- bazarr_deploy
    - dozzle_deploy
    - filebrowser_deploy
    #- ghost_deploy
    #- gitea_deploy
    #- gotify_deploy
    #- grafana_deploy
    #- homepage_deploy
    #- jellyfin_deploy
    #- jellyseerr_deploy
    #- kavita_deploy
    #- loki_deploy
    #- mariadb_deploy
    #- memos_deploy
    #- mkvtoolnix_deploy
    #- myspeed_deploy
    #- node-exporter_deploy
    #- overseerr_deploy
    #- paperless-ngx_deploy
    #- photoprism_deploy
    #- plex_deploy
    #- postgresql_deploy
    #- prometheus-podman-exporter_deploy
    #- prometheus_deploy
    #- prowlarr_deploy
    #- pulse_deploy
    #- radarr_deploy
    #- redis_deploy
    #- sonarr_deploy
    #- syncthing_deploy
    #- tautulli_deploy
    #- transmission_deploy
    #- ufw_install
    #- umami_deploy
    #- uptime-kuma_deploy
    #- vaultwarden_deploy
